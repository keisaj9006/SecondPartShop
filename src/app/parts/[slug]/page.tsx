import { notFound } from "next/navigation";
import Link from "next/link";
import { ArrowLeft,Check,MapPin,ShieldCheck,ShoppingBag,Truck } from "lucide-react";
import { Header } from "@/components/header";
import { PartMark } from "@/components/part-mark";
import { getListingBySlug } from "@/lib/data/listings";
export default async function PartPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const item=await getListingBySlug(slug);if(!item)notFound();return <><Header/><main className="mx-auto max-w-7xl px-4 py-8 sm:px-6"><Link href="/#marketplace" className="mb-6 inline-flex items-center gap-2 text-sm font-bold"><ArrowLeft size={16}/>Back to results</Link><div className="grid gap-8 lg:grid-cols-[1.05fr_.95fr]"><div className="min-h-[520px] overflow-hidden rounded-[28px] border border-black/10"><PartMark code={item.accent}/></div><div><div className="flex gap-2"><span className="rounded-full bg-[#e8eee9] px-3 py-1 text-xs font-bold">{item.condition}</span><span className="rounded-full bg-[#e8eee9] px-3 py-1 text-xs font-bold">{item.stock} in stock</span></div><h1 className="mt-5 text-4xl font-black tracking-[-.045em]">{item.title}</h1><p className="mt-2 font-mono text-sm text-[#63706a]">Gearbox {item.code} · OEM {item.oem}</p><p className="mt-7 text-4xl font-black">£{item.price.toLocaleString()}</p><p className="text-sm text-[#63706a]">Price includes VAT</p><div className="mt-7 rounded-2xl border border-black/10 bg-white p-5"><p className="font-bold">Compatibility confirmed for</p><ul className="mt-3 space-y-2">{item.compatibility.map(v=><li key={v} className="flex items-center gap-2 text-sm"><Check size={16} className="text-[#287154]"/>{v}</li>)}</ul><button className="mt-4 text-sm font-bold underline">Check against my exact vehicle</button></div><button className="mt-5 flex w-full items-center justify-center gap-2 rounded-xl bg-[#d4f44d] py-4 font-black"><ShoppingBag size={19}/>Add to basket</button><div className="mt-6 grid gap-3 rounded-2xl bg-[#173c31] p-5 text-sm text-white"><span className="flex items-center gap-2"><ShieldCheck className="text-[#d4f44d]" size={18}/>{item.seller.name} · {item.seller.rating}/5 verified</span><span className="flex items-center gap-2"><MapPin className="text-[#d4f44d]" size={18}/>{item.seller.location}, United Kingdom</span><span className="flex items-center gap-2"><Truck className="text-[#d4f44d]" size={18}/>{item.dispatch}</span></div></div></div></main></>}
