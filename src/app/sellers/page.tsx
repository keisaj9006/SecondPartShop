import Link from "next/link";
import { MapPin,ShieldCheck } from "lucide-react";
import { Header } from "@/components/header";
import { getSellers } from "@/lib/data/marketplace";
export const dynamic="force-dynamic";
export default async function SellersPage(){const sellers=await getSellers();return <><Header/><main className="mx-auto max-w-7xl px-4 py-12 sm:px-6"><p className="text-xs font-black uppercase tracking-[.2em] text-[#287154]">UK supplier network</p><h1 className="mt-3 text-4xl font-black tracking-[-.045em]">Sellers</h1><p className="mt-2 text-[#63706a]">Garages, breakers and transmission specialists listing on SecondPart.</p>{sellers.length?<div className="mt-8 grid gap-5 md:grid-cols-2 lg:grid-cols-3">{sellers.map(s=><Link href={`/seller/${s.slug}`} key={s.id} className="rounded-3xl border border-black/10 bg-white p-6 transition hover:-translate-y-1 hover:shadow-xl"><div className="flex items-center justify-between"><span className="grid h-12 w-12 place-items-center rounded-2xl bg-[#173c31] text-xl font-black text-[#d4f44d]">{s.businessName.charAt(0)}</span>{s.verified&&<span className="flex items-center gap-1 rounded-full bg-emerald-50 px-3 py-1 text-xs font-bold text-emerald-800"><ShieldCheck size={14}/>Verified</span>}</div><h2 className="mt-6 text-xl font-black">{s.businessName}</h2><p className="mt-2 line-clamp-2 text-sm leading-6 text-[#63706a]">{s.description}</p><p className="mt-5 flex items-center gap-2 text-sm font-semibold"><MapPin size={16}/>{s.location}</p></Link>)}</div>:<div className="mt-8 rounded-2xl bg-amber-50 p-5 text-sm text-amber-900">No sellers are available. Configure Supabase and run the development seed.</div>}</main></>}
