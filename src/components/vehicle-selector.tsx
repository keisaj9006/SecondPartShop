"use client";
import { useMemo,useState } from "react";
import { useRouter } from "next/navigation";
import type { Vehicle } from "@/lib/types";
const unique=(values:string[])=>[...new Set(values)];
export function VehicleSelector({vehicles,selectedId,baseParams}:{vehicles:Vehicle[];selectedId?:string;baseParams:Record<string,string>}){const selected=vehicles.find(v=>v.id===selectedId);const [make,setMake]=useState(selected?.make??"");const [model,setModel]=useState(selected?.model??"");const [generation,setGeneration]=useState(selected?.generation??"");const [year,setYear]=useState(selected?String(selected.year):"");const [engine,setEngine]=useState(selected?.engine??"");const [gearbox,setGearbox]=useState(selected?`${selected.gearboxFamily}|${selected.gearboxCode}`:"");const router=useRouter();const byMake=useMemo(()=>vehicles.filter(v=>v.make===make),[vehicles,make]);const byModel=byMake.filter(v=>v.model===model);const byGeneration=byModel.filter(v=>v.generation===generation);const byYear=byGeneration.filter(v=>String(v.year)===year);const byEngine=byYear.filter(v=>v.engine===engine);const finalVehicle=byEngine.find(v=>`${v.gearboxFamily}|${v.gearboxCode}`===gearbox);const choose=(id?:string)=>{const params=new URLSearchParams(baseParams);if(id)params.set("vehicle",id);else params.delete("vehicle");router.push(`/?${params.toString()}#marketplace`);};const control="min-w-0 rounded-xl border border-black/12 bg-white px-3 py-3 text-sm outline-none focus:ring-2 focus:ring-[#173c31] disabled:bg-black/5";return <div><div className="grid grid-cols-2 gap-2 sm:grid-cols-3"><select aria-label="Make" className={control} value={make} onChange={e=>{setMake(e.target.value);setModel("");setGeneration("");setYear("");setEngine("");setGearbox("");}}><option value="">Make</option>{unique(vehicles.map(v=>v.make)).map(v=><option key={v}>{v}</option>)}</select><select aria-label="Model" className={control} value={model} disabled={!make} onChange={e=>{setModel(e.target.value);setGeneration("");setYear("");setEngine("");setGearbox("");}}><option value="">Model</option>{unique(byMake.map(v=>v.model)).map(v=><option key={v}>{v}</option>)}</select><select aria-label="Generation" className={control} value={generation} disabled={!model} onChange={e=>{setGeneration(e.target.value);setYear("");setEngine("");setGearbox("");}}><option value="">Generation</option>{unique(byModel.map(v=>v.generation)).map(v=><option key={v}>{v}</option>)}</select><select aria-label="Year" className={control} value={year} disabled={!generation} onChange={e=>{setYear(e.target.value);setEngine("");setGearbox("");}}><option value="">Year</option>{unique(byGeneration.map(v=>String(v.year))).map(v=><option key={v}>{v}</option>)}</select><select aria-label="Engine" className={control} value={engine} disabled={!year} onChange={e=>{setEngine(e.target.value);setGearbox("");}}><option value="">Engine</option>{unique(byYear.map(v=>v.engine)).map(v=><option key={v}>{v}</option>)}</select><select aria-label="Gearbox" className={control} value={gearbox} disabled={!engine} onChange={e=>setGearbox(e.target.value)}><option value="">Gearbox</option>{byEngine.map(v=><option key={v.id} value={`${v.gearboxFamily}|${v.gearboxCode}`}>{v.gearboxFamily} · {v.gearboxCode}</option>)}</select></div><div className="mt-3 flex gap-3"><button type="button" disabled={!finalVehicle} onClick={()=>choose(finalVehicle?.id)} className="rounded-xl bg-[#d4f44d] px-5 py-3 text-sm font-black disabled:cursor-not-allowed disabled:opacity-50">Show compatible parts</button>{selectedId&&<button type="button" onClick={()=>choose()} className="text-sm font-bold underline">Clear vehicle</button>}</div></div>}
