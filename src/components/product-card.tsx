"use client";
import Link from "next/link";
import { Heart,MapPin,ShieldCheck } from "lucide-react";
import type { Listing } from "@/lib/types";
import { PartMark } from "./part-mark";
export function ProductCard({item,saved,onSave}:{item:Listing;saved:boolean;onSave:()=>void}){return <article className="group overflow-hidden rounded-[22px] border border-black/10 bg-white transition hover:-translate-y-1 hover:shadow-[0_18px_50px_rgba(18,34,29,.12)]"><div className="relative h-52"><PartMark code={item.accent}/><button onClick={onSave} aria-label="Save part" className={`absolute right-3 top-3 rounded-full p-2.5 shadow-sm ${saved?"bg-[#173c31] text-white":"bg-white text-[#173c31]"}`}><Heart size={18} fill={saved?"currentColor":"none"}/></button></div><div className="p-5"><div className="mb-3 flex items-center justify-between"><span className="rounded-full bg-[#eef1eb] px-2.5 py-1 text-[11px] font-bold uppercase tracking-wide">{item.condition}</span><span className="font-mono text-xs text-[#63706a]">{item.code}</span></div><Link href={`/parts/${item.slug}`}><h3 className="text-lg font-bold tracking-tight group-hover:underline">{item.title}</h3></Link><p className="mt-1.5 text-sm text-[#63706a]">OEM {item.oem}</p><div className="mt-4 flex items-center gap-1.5 text-xs text-[#63706a]"><ShieldCheck size={15} className="text-[#287154]"/><span className="font-semibold text-[#173c31]">Verified seller</span><span>·</span><MapPin size={13}/>{item.seller.location}</div><div className="mt-5 flex items-end justify-between border-t border-black/8 pt-4"><div><span className="text-2xl font-black">£{item.price.toLocaleString()}</span><p className="text-xs text-[#63706a]">incl. VAT</p></div><Link href={`/parts/${item.slug}`} className="rounded-full bg-[#173c31] px-4 py-2.5 text-sm font-bold text-white hover:bg-[#245b49]">View part</Link></div></div></article>}
