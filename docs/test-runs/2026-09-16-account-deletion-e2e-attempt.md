# SecondPart account-deletion E2E attempt — 2026-09-16

## Scope

This records an attempted destructive QA account-deletion run on `rebuild-nextjs` using a disposable buyer identity created specifically for deletion testing.

No Production environment, real customer account, real financial transaction or retained customer evidence was used. No password, access token, mailbox address, service-role key or CRON secret is recorded here.

## Disposable identity

- disposable QA profile: `ae6d5751-b186-4597-abe1-d41641e0f230`
- profile role: `buyer`
- profile display label: `Delete QA2 20260916`
- account created through the normal SecondPart signup UI
- signup confirmation mail was received from the configured Supabase project
- final direct Auth verification confirmed `email_confirmed_at` is set

## Observed auth redirect issue

One signup confirmation mail generated during this attempt contained `redirect_to=http://localhost:3000`. Opening the one-time confirmation link still verified the identity before the localhost redirect failed, but a real user must not be sent to localhost.

A later password-reset request generated by the current Preview code used the correct Preview branch return origin. This means the current auth-origin logic needs a targeted regression check specifically for signup confirmation before the redirect issue can be considered closed.

## Deletion-run status

The full Scenario A deletion run was **not completed** and must not be classified as PASS.

The runbook requires the normal UI for account creation and deletion request, plus the normal `/api/commerce/maintenance` worker. Manual deletion-state RPCs, manual status edits and manual Auth deletion are explicitly prohibited for a release PASS.

A fresh disposable signup attempt intended to obtain a continuous authenticated confirmation session was blocked by Supabase Auth with `email rate limit exceeded`. Multiple normal UI retries remained rate-limited. No manual database workaround was used to manufacture a passing E2E result.

No deletion request was manually inserted or advanced. No `claim_account_deletion_request`, `prepare_claimed_account_deletion`, `complete_account_deletion_request` or direct Auth hard-delete was used to claim release evidence.

## Classification

- **VERIFIED:** disposable QA account creation through normal UI; profile creation; confirmation mail receipt; direct Auth confirmation state for the disposable profile.
- **DEFECT / NEEDS TARGETED REGRESSION:** signup confirmation return origin produced a localhost redirect in one observed run, while the current reset flow produced the correct Preview branch origin.
- **BLOCKED / RETRY REQUIRED:** destructive Scenario A deletion E2E is blocked by the current Supabase Auth email-send rate limit before a continuous normal UI session can be established.
- **NOT CLAIMED:** deletion request UI acceptance, maintenance-worker hard deletion, Auth absence, Storage cleanup, data cleanup and retry idempotency.

## Next safe action

After the Supabase Auth email-send rate limit clears, create a new disposable buyer through the current Preview branch, confirm through the generated Preview callback, create harmless disposable personal data, submit deletion through Account → Security, run the normal authenticated maintenance endpoint and verify Auth/DB/Storage plus a second maintenance pass.
